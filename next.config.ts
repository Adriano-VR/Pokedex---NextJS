import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['raw.githubusercontent.com'], // Adicione aqui os domínios das imagens que deseja carregar
  },
};

export default nextConfig;
